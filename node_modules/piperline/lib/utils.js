'use strict';

Object.defineProperty(exports, '__esModule', {
    value: true
});

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

var _lodash_basebind = require('lodash._basebind');

var _lodash_basebind2 = _interopRequireDefault(_lodash_basebind);

exports['default'] = {
    executeAsAsync: function executeAsAsync(handler) {
        if (process) {
            process.nextTick(handler);
        } else {
            setTimeout(handler, 4);
        }
    },

    bindFunc: function bindFunc(func, thisArg) {
        for (var _len = arguments.length, args = Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
            args[_key - 2] = arguments[_key];
        }

        return (0, _lodash_basebind2['default'])([func, null, args, null, thisArg]);
    },

    isFunction: function isFunction(target) {
        return typeof target === 'function';
    },

    isError: function isError(target) {
        return target instanceof Error;
    }
};
module.exports = exports['default'];